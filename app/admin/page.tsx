import AdminWebDashboard from "./AdminWeb";

// Opt out of static generation for this page
export const dynamic = 'force-dynamic';

export default function AdminDashboard() {
  return <AdminWebDashboard />;
}
