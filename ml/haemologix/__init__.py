"""Haemologix decision-intelligence models.

Outcome-prediction models (per task) trained on simulator + harvested real data,
served over FastAPI to the TypeScript agents via lib/ml/modelClient.ts.
"""

__version__ = "1.0.0"
